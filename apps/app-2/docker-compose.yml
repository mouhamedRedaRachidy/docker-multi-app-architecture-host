services:

  app-2:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner

    image: landing-dark:v1

    container_name: app-2

    restart: unless-stopped

    expose:
      - "3000"

    networks:
      - web_network

networks:
  web_network:
    external: true